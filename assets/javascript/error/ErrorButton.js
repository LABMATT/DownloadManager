// ON INIT THEN REG THE CLOSE BUTTON
function init() {
    const button = document.getElementById("closeButton")

    button.addEventListener("click", closeWindow);
}

// Close window on button press.
function closeWindow() {

    
}